{% block navigation %}
	<div class="nav-bg"></div>
	<div class="navigation">
		<div class="logo">
			<a href="{{ path('homepage') }}"><h1>Awareit</h1></a>
		</div>
		<div class="nav-point">
			<a href="{{ path('homepage') }}">Home</a>
		</div>
		<div class="nav-point">
			<a href="{{ path('homepage') }}#service">Service</a>
		</div>
		<div class="nav-point">
			<a href="{{ path('homepage') }}#pricing">Preise</a>
		</div>
		<div class="nav-point">
			{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
		        <a href="{{ path('user_backend') }}" class="username">{{ app.user.username }}</a>
		        <span> | </span>
		        <a href="{{ path('fos_user_security_logout') }}">
		            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
		        </a>
		    {% else %} 
		        <a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
		    {% endif %}
		</div>
	</div>
	<div class="navigation-mobile">
		<div class="logo">
			<a href="{{ path('homepage') }}"><h1>Awareit</h1></a>
		</div>
		<a class="nav-button" role="button" data-toggle="collapse" href="#collapseNavPoints" aria-expanded="false" aria-controls="collapseNavPoints">
			<i class="fa fa-bars" aria-hidden="true"></i>
		</a>
		<div class="collapse" id="collapseNavPoints">
			<div class="nav-point-background"></div>
			<div class="nav-point">
				<a href="{{ path('homepage') }}">Home</a>
			</div>
			<div class="nav-point">
				<a href="{{ path('homepage') }}#service">Service</a>
			</div>
			<div class="nav-point">
				<a href="{{ path('homepage') }}#pricing">Preise</a>
			</div>
			<div class="nav-point">
				{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
			        <a href="{{ path('user_backend') }}" class="username">{{ app.user.username }}</a>
			    {% endif %}
			</div>
			<div class="nav-point">
				{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
			        <a href="{{ path('fos_user_security_logout') }}">
			            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
			        </a>
			    {% else %} 
			        <a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
			    {% endif %}
			</div>
		</div>
	</div>

{% endblock %} 